#include <stdio.h>
#include <stdlib.h>

int main()
{
    int numero;
    printf("ingrese un numero: ");
    scanf("%d", &numero);
    printf("usted ingreso el numero: %d", numero);
    return 0;
}

//scanf("%d", &numero);(!!solo en scanf())
